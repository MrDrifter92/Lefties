<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Big Bay Minimart Outlet</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-pink: #E6007A;
            --light-pink-glow: rgba(230, 0, 122, 0.6);
            --dark-bg: #0D1117;
            --mid-dark-bg: #161B22;
            --light-text: #e5e7eb;
        }

        body {
            font-family: 'Nunito', sans-serif;
            background-color: var(--dark-bg);
            color: var(--light-text);
        }

        .deep-glass {
            position: relative;
            overflow: hidden;
            background: linear-gradient(145deg, rgba(59, 30, 41, 0.2), rgba(39, 17, 24, 0.4));
            border: 1px solid rgba(255, 255, 255, 0.15);
            border-radius: 1.5rem;
            box-shadow: 
                0 0 25px var(--light-pink-glow),
                inset 0 2px 2px rgba(255, 255, 255, 0.07),
                inset 0 -2px 2px rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(18px);
            -webkit-backdrop-filter: blur(18px);
        }
        
        .bubble-container { position: absolute; top: 0; left: 0; width: 100%; height: 100%; overflow: hidden; z-index: 0; }
        .bubble {
            position: absolute;
            background: radial-gradient(circle at 30% 30%, rgba(255,255,255,0.1), var(--light-pink-glow));
            border-radius: 50%;
            box-shadow: inset 0 3px 5px rgba(255,255,255,0.05), 0 0 20px var(--light-pink-glow);
            animation: float 20s infinite ease-in-out;
        }
        @keyframes float {
            0% { transform: translateY(10vh) scale(0.8); opacity: 0; }
            25% { opacity: 0.7; }
            50% { transform: translateY(-15vh) scale(1.1); }
            75% { opacity: 0.7; }
            100% { transform: translateY(-30vh) scale(0.9); opacity: 0; }
        }
        .product-card {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        .product-card:last-child {
            border-bottom: none;
        }
        .cart-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.5rem 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        .cart-item:last-child {
            border-bottom: none;
        }
    </style>
</head>
<body class="antialiased">

    <main class="relative z-10 py-12 md:py-20">
        <div class="bubble-container">
            <div class="bubble" style="width: 250px; height: 250px; top: 10%; left: -100px; animation-duration: 45s;"></div>
            <div class="bubble" style="width: 150px; height: 150px; bottom: 10%; right: -80px; animation-duration: 35s; animation-delay: 5s;"></div>
        </div>

        <div class="container mx-auto px-6 max-w-4xl">
            <div class="text-center mb-12">
                <!-- Replaced the h1 with the provided logo image -->
                <img src="https://raw.githubusercontent.com/MrDrifter92/Lefties/115adf2373c13f35db5fbd44ecbf5a6f5b0f9e73/IMG_4141.png" 
                     alt="The Big Bay Minimart Outlet" 
                     class="w-full max-w-lg mx-auto mb-4"
                     onerror="this.onerror=null;this.src='https://placehold.co/500x150/E6007A/ffffff?text=Minimart+Logo';" />
                <p class="mt-2 text-lg text-gray-400 max-w-2xl mx-auto">Hervey Bay’s cheekiest bargain shelf. Same flavour, smaller stash, better price.</p>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                <!-- Product List -->
                <div class="lg:col-span-2 space-y-12">
                    
                    <!-- Vaporising Juice Blends -->
                    <div class="deep-glass p-6 md:p-8">
                        <h2 class="text-3xl font-bold text-center text-pink-400 mb-6">🧃 Vaporising Juice Blends</h2>
                        <p class="text-gray-300 text-center mb-6">For the vapers who like their clouds crisp and their budgets tighter.</p>
                        <div id="vapor-products" class="space-y-4">
                            <!-- Product items will be dynamically inserted here by JavaScript -->
                        </div>
                    </div>

                    <!-- Nicotine Chewing Gum -->
                    <div class="deep-glass p-6 md:p-8">
                        <h2 class="text-3xl font-bold text-center text-pink-400 mb-6">🍬 Nicotine Chewing Gum</h2>
                        <p class="text-gray-300 text-center mb-6">Same hit, half the chew. Or go big and stay stocked.</p>
                        <div id="gum-products" class="space-y-4">
                            <!-- Product items will be dynamically inserted here by JavaScript -->
                        </div>
                    </div>
                </div>

                <!-- Shopping Cart -->
                <div class="lg:col-span-1">
                    <div id="cart-container" class="deep-glass p-6 sticky top-8">
                        <h2 class="text-3xl font-bold text-center text-pink-400 mb-6">Your Cart</h2>
                        <div id="cart-items" class="min-h-[100px]">
                            <!-- Cart items will be dynamically added here -->
                            <p class="text-center text-gray-500">Your cart is empty.</p>
                        </div>
                        <div class="mt-6 pt-4 border-t border-gray-700">
                            <div class="flex justify-between items-center text-2xl font-bold text-white">
                                <span>Total:</span>
                                <span id="cart-total">$0</span>
                            </div>
                            <button id="checkout-button" class="mt-4 w-full bg-pink-600 hover:bg-pink-700 text-white font-bold py-3 px-4 rounded-xl transition duration-300">
                                Proceed to Checkout
                            </button>
                            <button id="clear-cart-button" class="mt-2 w-full text-pink-400 hover:text-white text-sm">
                                Clear Cart
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="bg-black py-8 mt-12">
        <div class="container mx-auto px-6 text-center">
            <p class="text-sm text-gray-400">&copy; 2025 The Big Bay Minimart Outlet. All Rights Reserved.</p>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Define the products with their details and prices
            const products = [
                { id: 'juice-barry-original', name: 'Barry Green Juice – Original', flavour: 'Green Apple Ice', size: '120mL', price: 50, type: 'vapor' },
                { id: 'juice-cloudyard', name: 'CloudYard Glo', flavour: 'Green Apple Ice', size: '60mL', price: 30, type: 'vapor' },
                { id: 'juice-barry-sunrise', name: 'Barry Green Juice – Sunrise', flavour: 'Mango Guava Chill', size: '120mL', price: 50, type: 'vapor' },
                { id: 'juice-tropikmist', name: 'TropikMist Lite', flavour: 'Mango Guava Chill', size: '60mL', price: 30, type: 'vapor' },
                { id: 'juice-barry-midnight', name: 'Barry Green Juice – Midnight', flavour: 'Grape Blackcurrant', size: '120mL', price: 50, type: 'vapor' },
                { id: 'juice-smokedrip', name: 'SmokeDrip Mini', flavour: 'Grape Blackcurrant', size: '60mL', price: 30, type: 'vapor' },
                { id: 'juice-barry-frostbite', name: 'Barry Green Juice – Frostbite', flavour: 'Menthol Mint Pop', size: '120mL', price: 50, type: 'vapor' },
                { id: 'juice-polar', name: 'PolarFume Snap', flavour: 'Menthol Mint Pop', size: '60mL', price: 30, type: 'vapor' },
                { id: 'juice-barry-candyland', name: 'Barry Green Juice – Candyland', flavour: 'Strawberry Lolly', size: '120mL', price: 50, type: 'vapor' },
                { id: 'juice-sweetclouds', name: 'SweetClouds Snap', flavour: 'Strawberry Lolly', size: '60mL', price: 30, type: 'vapor' },
                { id: 'gum-barry-mint', name: 'Barry Gum – Arctic Mint', flavour: 'Icy Spearmint', size: '40 pieces', price: 50, type: 'gum' },
                { id: 'gum-chillchew', name: 'ChillChew Quick Fix', flavour: 'Icy Spearmint', size: '20 pieces', price: 30, type: 'gum' },
                { id: 'gum-barry-tropic', name: 'Barry Gum – Tropic Boost', flavour: 'Pineapple Mango', size: '40 pieces', price: 50, type: 'gum' },
                { id: 'gum-islandbite', name: 'Island Bite Minis', flavour: 'Pineapple Mango', size: '20 pieces', price: 30, type: 'gum' },
                { id: 'gum-barry-cola', name: 'Barry Gum – Cola Rush', flavour: 'Fizzy Cola', size: '40 pieces', price: 50, type: 'gum' },
                { id: 'gum-fizzdrop', name: 'FizzDrop Compact', flavour: 'Fizzy Cola', size: '20 pieces', price: 30, type: 'gum' },
                { id: 'gum-barry-berry', name: 'Barry Gum – Berry Blaze', flavour: 'Mixed Berry', size: '40 pieces', price: 50, type: 'gum' },
                { id: 'gum-gumburst', name: 'Gumburst Twins', flavour: 'Mixed Berry', size: '20 pieces', price: 30, type: 'gum' },
                { id: 'gum-barry-menthol', name: 'Barry Gum – Classic Menthol', flavour: 'Strong Mint', size: '40 pieces', price: 50, type: 'gum' },
                { id: 'gum-snapmint', name: 'SnapMint HalfPack', flavour: 'Strong Mint', size: '20 pieces', price: 30, type: 'gum' }
            ];

            const vaporProductsContainer = document.getElementById('vapor-products');
            const gumProductsContainer = document.getElementById('gum-products');
            const cartItemsContainer = document.getElementById('cart-items');
            const cartTotalDisplay = document.getElementById('cart-total');
            const checkoutButton = document.getElementById('checkout-button');
            const clearCartButton = document.getElementById('clear-cart-button');

            let cart = {};

            // Function to render a product item
            function renderProduct(product) {
                const productElement = document.createElement('div');
                productElement.className = 'product-card';
                productElement.innerHTML = `
                    <div>
                        <h3 class="font-bold text-lg">${product.name}</h3>
                        <p class="text-sm text-gray-400">${product.flavour} &bull; ${product.size}</p>
                        <p class="font-bold text-xl text-pink-400 mt-1">$${product.price}</p>
                    </div>
                    <div class="flex items-center space-x-2">
                        <input type="number" id="qty-${product.id}" value="1" min="1" class="w-16 p-2 rounded-lg text-black bg-gray-200 text-center">
                        <button data-id="${product.id}" class="add-to-cart-btn bg-pink-600 hover:bg-pink-700 text-white font-bold p-2 rounded-lg transition duration-300">
                            Add
                        </button>
                    </div>
                `;
                return productElement;
            }

            // Function to render all products
            function renderProducts() {
                vaporProductsContainer.innerHTML = '';
                gumProductsContainer.innerHTML = '';
                products.forEach(product => {
                    if (product.type === 'vapor') {
                        vaporProductsContainer.appendChild(renderProduct(product));
                    } else if (product.type === 'gum') {
                        gumProductsContainer.appendChild(renderProduct(product));
                    }
                });
            }

            // Function to update the cart display and total
            function updateCart() {
                cartItemsContainer.innerHTML = '';
                let total = 0;
                const cartKeys = Object.keys(cart);
                
                if (cartKeys.length === 0) {
                    cartItemsContainer.innerHTML = '<p class="text-center text-gray-500">Your cart is empty.</p>';
                } else {
                    cartKeys.forEach(productId => {
                        const item = cart[productId];
                        const itemTotal = item.price * item.quantity;
                        total += itemTotal;
                        const cartItemElement = document.createElement('div');
                        cartItemElement.className = 'cart-item';
                        cartItemElement.innerHTML = `
                            <div>
                                <p class="font-bold">${item.name}</p>
                                <p class="text-sm text-gray-400">Qty: ${item.quantity}</p>
                            </div>
                            <span class="font-bold">$${itemTotal}</span>
                        `;
                        cartItemsContainer.appendChild(cartItemElement);
                    });
                }
                
                cartTotalDisplay.textContent = `$${total}`;
            }

            // Event listener for adding items to the cart
            document.body.addEventListener('click', (event) => {
                if (event.target.classList.contains('add-to-cart-btn')) {
                    const productId = event.target.dataset.id;
                    const quantityInput = document.getElementById(`qty-${productId}`);
                    const quantity = parseInt(quantityInput.value) || 1;

                    if (quantity > 0) {
                        const product = products.find(p => p.id === productId);
                        if (cart[productId]) {
                            cart[productId].quantity += quantity;
                        } else {
                            cart[productId] = {
                                ...product,
                                quantity: quantity
                            };
                        }
                        updateCart();
                    }
                }
            });

            // Event listener for the checkout button
            checkoutButton.addEventListener('click', () => {
                if (Object.keys(cart).length > 0) {
                    const message = `
                        Thank you for your order!
                        Your total is ${cartTotalDisplay.textContent}.
                        We appreciate your business!
                    `;
                    // Using a custom "modal" since we cannot use alert()
                    const modal = document.createElement('div');
                    modal.className = 'fixed inset-0 bg-black bg-opacity-75 flex items-center justify-center p-4';
                    modal.innerHTML = `
                        <div class="deep-glass p-8 rounded-xl max-w-sm text-center">
                            <h3 class="text-2xl font-bold text-pink-400 mb-4">Checkout Complete!</h3>
                            <p class="text-white whitespace-pre-wrap">${message}</p>
                            <button id="close-modal" class="mt-6 bg-pink-600 hover:bg-pink-700 text-white font-bold py-2 px-4 rounded-xl transition duration-300">
                                Close
                            </button>
                        </div>
                    `;
                    document.body.appendChild(modal);

                    document.getElementById('close-modal').addEventListener('click', () => {
                        modal.remove();
                        // Optional: Clear the cart after checkout
                        cart = {};
                        updateCart();
                    });

                } else {
                    // Placeholder for a message box telling the user the cart is empty
                    console.log('Cart is empty.');
                }
            });
            
            // Event listener for the clear cart button
            clearCartButton.addEventListener('click', () => {
                cart = {};
                updateCart();
            });


            // Initial render
            renderProducts();
            updateCart();
        });
    </script>
</body>
</html>
